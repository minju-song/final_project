<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.holoyolo.app.memberFinanceInfo.mapper.MemberFinanceInfoMapper">


	<select id="getCardInfo" resultType="MemberFinanceInfoVO">
		SELECT card_no, card_company 
		FROM member_finance_info 
		WHERE member_id = #{memberId} 
		AND use_yn = 'y'
	</select>
	
	
	<select id="selectMemberFinanceInfo" resultType="MemberFinanceInfoVO">
		SELECT *
		FROM MEMBER_FINANCE_INFO
		WHERE SETTING_DATE = (SELECT MAX(SETTING_DATE)
												FROM MEMBER_FINANCE_INFO
												WHERE MEMBER_ID = #{memberId})
		AND USE_YN = 'Y'
	</select>
	
	<update id="insertOrUpdateFinanceInfo" parameterType="MemberFinanceInfoVO" statementType="CALLABLE">
  execute p_userAccountController(#{memberId}, #{bankname}, #{account})
  </update>

</mapper>